# Empêche tout accès web direct au code applicatif (déjà bloqué par la racine .htaccess)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
